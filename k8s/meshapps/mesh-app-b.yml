apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: mesh-app-b
    app.kubernetes.io/component: mesh-app-b
    app.kubernetes.io/instance: mesh-app-b
    app.kubernetes.io/part-of: meshapps
    app.openshift.io/runtime: python
  name: mesh-app-b
  namespace: meshapps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mesh-app-b
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: mesh-app-b
        version: v1
        sidecar.istio.io/inject: "true"
    spec:
      containers:
        - image: quay.io/nlembers/distributed-tracing-mesh-app-b:latest
          imagePullPolicy: Always
          name: mesh-app-b
          env:
            - name: PORT
              value: "8080"
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
          securityContext:
            privileged: false
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: mesh-app-b
  name: mesh-app-b
  namespace: meshapps
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: mesh-app-b
  sessionAffinity: None
  type: ClusterIP